# Discord Exporter 프로젝트 규칙

이 문서는 프로젝트 개발 시 반드시 준수해야 하는 상시 규칙입니다.

---

## 1. 보안 규칙 (토큰 처리)

### 절대 금지 사항
- **토큰을 절대 평문으로 출력하지 않는다**
  - UI 화면 (Entry, Label, Text 등)
  - 로그/콘솔 출력 (print, logging)
  - 에러 메시지 (Exception, traceback)
  - README/문서
  - 소스코드 상수/주석

### 토큰 표시 규칙
- 사용자에게 토큰 표시 시: `xxxx...xxxx` (앞 4자리...뒤 4자리)
- 실행 커맨드 로그 시: `******` 또는 `<TOKEN>`
- 에러 메시지: `sanitize_error_message()` 함수로 필터링

### 토큰 노출 위험 지점 체크리스트
- [ ] `cli.py`: 사용자 입력 토큰이 로그/화면에 직접 출력되지 않는가 (`getpass` 사용, `print`/`logging` 직접 호출 금지)
- [ ] `exporter.py`: `_run_command()` 에서 display_cmd 생성 시 마스킹
- [ ] `exporter.py`: subprocess 결과 출력 시 `sanitize_error_message()` 적용
- [ ] `config.py`: `mask_token()` 함수 사용
- [ ] GUI: 토큰 입력 필드는 `show="*"` 속성 사용
- [ ] GUI: 상태 표시에 토큰 포함 금지
- [ ] GUI: 실행 로그에 마스킹된 커맨드만 표시

---

## 2. 의존성 규칙

### 허용된 의존성
- **Python 표준 라이브러리만 사용**
  - `tkinter` - GUI (표준 라이브러리)
  - `argparse` - CLI 파싱
  - `subprocess` - 외부 프로세스 실행
  - `json` - 설정 파일
  - `pathlib`, `os` - 경로 처리
  - `datetime` - 시간 처리
  - `re` - 정규식
  - `getpass` - 보안 입력
  - `threading` - 비동기 작업 (GUI용)
  - `queue` - GUI 로그 큐 (스레드 간 통신)
  - `concurrent.futures` - 채널 접근성 병렬 테스트 (GUI용)
  - `hashlib` - 미디어 파일명 해시 계산 (avatar/emoji 정리)
  - `platform` - OS 감지 (DCE 경로 자동탐지, 권한 처리)
  - `urllib.parse` - URL 파싱 (미디어 파일명 계산)
  - `time` - 채널 목록 조회 경과 시간 표시 (GUI용)

### 금지된 의존성
- `python-dotenv` → 직접 파싱 구현 완료
- `requests`, `httpx` → 불필요 (DCE가 처리)
- `PyQt`, `wxPython` → Tkinter만 사용
- 기타 외부 패키지 → 추가 전 반드시 논의

---

## 3. 아키텍처 규칙

### 코어 로직 재사용
```
discord_exporter/
├── config.py      # 설정 관리 (공용)
├── utils.py       # 유틸리티 (공용)
├── exporter.py    # DCE 호출 로직 (공용)
├── cli.py         # CLI 프론트엔드
└── gui.py         # GUI 프론트엔드
```

- `exporter.py`는 CLI/GUI 모두에서 재사용
- GUI는 프론트엔드로만 구현 (비즈니스 로직 중복 금지)
- 새 프론트엔드 추가 시 `exporter.py` 수정 최소화

### DCE 경로 처리 우선순위
1. 환경 변수 `DCE_PATH`
2. 설정 파일 `~/.discord-exporter/config.json`
3. 자동 탐지 (일반적인 설치 경로)

**하드코딩 절대 금지**

---

## 4. 개발 프로세스 규칙

### 변경 순서
1. **작은 변경** - 한 번에 하나의 기능/수정만
2. **검증** - 변경 후 즉시 테스트
3. **다음 단계** - 검증 완료 후에만 진행

### 커밋 전 체크리스트
- [ ] 토큰이 코드/로그에 노출되지 않는가?
- [ ] 외부 의존성을 추가하지 않았는가?
- [ ] 기존 CLI 기능이 정상 동작하는가?
- [ ] 에러 메시지에 민감 정보가 없는가?

---

## 5. GUI 구현 규칙 (Tkinter)

### 필수 사항
- `tkinter` 표준 라이브러리만 사용
- 토큰 입력 필드: `Entry(show="*")`
- 실행 중 UI 블로킹 방지: `threading` 사용
- 크로스 플랫폼 호환성 유지 (macOS/Windows)

### UI 표시 규칙
- 실행 커맨드 로그: 토큰은 `******`로 치환
- 상태 메시지: 토큰 미포함
- 에러 표시: `sanitize_error_message()` 적용

---

## 6. 파일별 책임

| 파일 | 책임 | 토큰 접근 |
|------|------|----------|
| `config.py` | 설정 저장/로드, 토큰 마스킹 | 저장/로드만 |
| `utils.py` | URL 파싱, 포맷 변환, 에러 필터링 | 필터링만 |
| `exporter.py` | DCE 호출, 커맨드 빌드 | 커맨드 빌드 시 |
| `cli.py` | CLI 인터페이스 | 사용자 입력 시 |
| `gui.py` | GUI 인터페이스 | 사용자 입력 시 |

---

## 변경 이력

| 날짜 | 변경 내용 |
|------|----------|
| 2025-01-13 | 초기 규칙 문서 작성 |
| 2026-06-08 | Doc-Sync 검증 완료 — 코드베이스(v1.1.0) 대비 정합성 확인. 주요 사항: `safe_mode` 필드(ExportOptions, GUI 전용), Markdown export(`md`/`obsidian` 포맷), 스트리밍 실행(`_run_command_streaming`) 모두 §1-§6 규칙 준수 확인됨 |
| 2026-06-09 | Doc-Sync 재검증 — MISMATCH 4건 수정: ①GUI_TEST_CHECKLIST 시나리오 10 예상 메시지(실제 코드 기준으로 교정), ②시나리오 4 예상 메시지(영문 ValueError 래핑 명시), ③README GUI 주요 기능에 채널 목록 조회·배치 내보내기·미디어 정리 항목 추가, ④exportdm `--include-threads` 무시 동작 명시 |
| 2026-06-10 | Doc-Sync 3차 검증 — MISMATCH 3건 수정: ①GUI_SPEC §3 상태 전이 다이어그램 "3초 후 자동" → "즉시 자동"으로 교정(코드에 after(3000) 없음) + 설명 주석 추가, ②GUI_SPEC §2 위젯 목록 누락 4개 추가(exclude_avatars, exclude_emojis, filter_text_only, filter_accessible), ③CORE_API_PLAN §3.3 default_log_callback 예시 코드 실제 구현(레벨별 prefix) 반영 |
| 2026-06-11 | Doc-Sync 4차 검증 — MISMATCH 2건 수정: ①GUI_SPEC §5 E4 오류 메시지 "채널 URL 또는 ID를 입력해주세요" → "내보내기 대상이 없습니다.\nDiscord URL을 입력하거나 채널을 체크해주세요."로 교정(gui.py `_validate_inputs` 실제 메시지), ②GUI_SPEC §5 E2 오류 메시지 "DCE 실행 파일 경로가 설정되지 않았습니다" → "DCE 경로가 설정되지 않았습니다."로 교정(utils.py `validate_dce_executable` 반환값) |
| 2026-06-12 | Doc-Sync 5차 검증 — MISMATCH 3건 수정: ①README GUI 오류표 "시작일이 종료일보다 늦습니다" → "늦거나 같습니다"로 교정(utils.py `validate_date_range` 실제 메시지), ②GUI_SPEC §4 시나리오 1 스텝 3-6 교정(비존재 `btn_settings`/설정 다이얼로그/[저장] → 상태바 "토큰 설정"+"찾아보기" 버튼 흐름으로), ③GUI_SPEC §4 시나리오 3 스텝 1 교정(비존재 `[URL/ID 전환]` 버튼 → `ent_channel_id` 직접 입력으로) |
| 2026-06-13 | Doc-Sync 6차 검증 — MISMATCH 4건 수정: ①GUI_SPEC §5 E5 메시지 교정(다이얼로그 없음 → `lbl_export_target` "URL 오류" 인라인 + E4 fallback), ②E6 메시지 교정("채널 ID 오류: Channel ID should be 17-20 digits, got N digits"로), ③E7 메시지 교정("날짜 형식이 올바르지 않습니다: '{date_str}'"로), ④E8 메시지 교정("폴더에 쓰기 권한이 없습니다: {path}"로). UNDOCUMENTED 2건 추가: ⑤GUI_SPEC §2 위젯 목록에 `lbl_export_target`·`cmb_threads` 추가. STALE 1건 수정: ⑥CORE_API_PLAN §3.1 ExportOptions 예시에 `safe_mode` 필드 추가 |
| 2026-06-17 | Doc-Sync 7차 검증 — MISMATCH 2건 수정: ①GUI_SPEC §2 `lbl_export_target` 기본값 교정(`"URL 또는 ID를 입력하세요"` → `"URL을 입력하세요"` — 위젯 생성 시 초기값 기준, `_update_export_target()` 첫 호출 후 변경됨을 명시), ②GUI_TEST_CHECKLIST 시나리오 3 예상 결과 교정(다이얼로그+로그 → `lbl_export_target` `"URL 오류"` 인라인 + 내보내기 실행 시 E4 다이얼로그로) |
| 2026-06-19 | Doc-Sync 8차 검증 — MISMATCH 2건 수정: ①README GUI 오류표 URL 에러 메시지 교정(`"URL 형식을 인식할 수 없습니다"` → `"URL 오류"` 인라인 — utils.py Python 예외 메시지가 GUI에 직접 노출되지 않음을 반영), ②GUI_TEST_CHECKLIST 시나리오 3 예상 결과에 `https://google.com/channels/...` 케이스 조기 반환 동작 주의사항 추가(`_on_parse_url`의 `"discord.com/channels" not in url` 조건으로 인해 "URL 오류" 미표시) |
| 2026-06-20 | Doc-Sync 9차 검증 — STALE_DOC 1건 수정: §2 허용된 의존성 목록에 실제 사용 중인 표준 라이브러리 6개 추가(`queue`, `concurrent.futures`, `hashlib`, `platform`, `urllib.parse`, `time`) — 규칙 위반 아님, 목록 불완전 보완 |
| 2026-06-22 | Doc-Sync 10차 검증 — MISMATCH 1건 수정: README §주요 기능 "macOS (Apple Silicon) 및 Windows 지원" → "macOS (Apple Silicon) / Windows / Linux 지원"으로 교정 (config.py `_auto_detect_dce()` Linux 분기, CI ubuntu-latest 매트릭스, pyproject.toml `OS Independent` 분류자 근거) |
| 2026-06-24 | Doc-Sync 11차 검증 — MISMATCH 2건 수정: ①README §다운로드 표 Linux 행 추가(`DiscordChatExporter.Cli.linux-x64.zip` — config.py Linux 자동탐지 경로·CI ubuntu-latest 근거), ②README §환경 변수 `macOS/Linux` 통합 예시 → macOS / Linux 개별 예시로 분리(Linux DCE 경로 `linux-x64` 반영) |
| 2026-06-28 | Doc-Sync 12차 검증 — MISMATCH 0건, 전체 84개 클레임 MATCH. 코드 버그 관찰: `gui.py` `_build_export_options_for_channel()` format_map 키 불일치(`"Text"` vs `"Plain Text"`, `"Markdown"` vs `"Markdown (Obsidian)"`) → 배치 내보내기에서 해당 포맷 선택 시 HTML_DARK로 silent fallback 발생. 문서 수정 불필요, 코드 수정 권고. |
| 2026-06-30 | Doc-Sync 13차 검증 — MISMATCH 0건, 전체 130개 클레임 MATCH. 코드 버그 수정: `gui.py` `_build_export_options_for_channel()` 내 별도 format_map 제거 → `self._get_export_format()` 재사용으로 교체. FORMAT_OPTIONS 표시명과 자동 동기화되어 배치 내보내기에서 "Plain Text"/"Markdown (Obsidian)" silent fallback 버그 해결. |
| 2026-07-01 | Doc-Sync 14차 검증 — MISMATCH 0건, 전체 142개 클레임 MATCH. 전 회차 대비 신규 코드 커밋 없음 확인. 이번 회차 추가 검증: GUI 위젯 이름 전체 교차 확인(lbl_token_status~txt_log 25개), `filter_accessible` ThreadPoolExecutor(max_workers=10) 동작, `safe_mode` 기본값 True, `exclude_avatars/emojis` 기본값 True, `ent_token show="*"`. |
| 2026-07-02 | Doc-Sync 15차 검증 — MISMATCH 0건, 전체 157개 클레임 MATCH (+15 신규). 신규 검증: `_run_command()` `<TOKEN>` 마스킹(CLAUDE.md §1 `<TOKEN>`/`******` 모두 허용 확인), `mask_token()` 앞4자리...뒤4자리 포맷, config.json+.env 이중 저장, `group_channels_by_type` backward-compat alias, Linux DCE 자동탐지 경로(`linux-x64`), `convert_json_to_markdown()` Obsidian 구조, `cleanup_avatar_emoji_files()` ↔ GUI exclude_avatars/emojis 연결, ExportOptions.safe_mode 이중 기본값(dataclass=False/GUI=True 의도적 차이). 관찰: git 워킹트리 7개 uncommitted 파일(마지막 커밋 2026-06-05) — 코드-문서 정합성에는 영향 없음. |
| 2026-07-03 | Doc-Sync 16차 검증 — MISMATCH 0건, 전체 172개 클레임 MATCH (+15 신규). 신규 검증: §2 허용 라이브러리 15개 실제 import 교차 확인(`queue`/`concurrent.futures`/`hashlib`/`platform`/`urllib.parse`/`time` 모두 실재), `_build_export_options_for_channel()` `_get_export_format()` 재사용 확인(2026-06-30 버그수정 반영), E1~E8 에러 메시지 8개 전수 재확인, `_test_channels_parallel()` dead code 관찰(gui.py:758 정의, 미호출 — 코드 정리 권고, 문서 수정 불필요), 전체 위젯 25개 재확인, CI 매트릭스(ubuntu+macos py3.9~3.13 + windows smoke) 재확인. |
| 2026-07-04 | Doc-Sync 17차 검증 — UNDOCUMENTED 1건 수정: `.env.example`의 `DCE_PATH` 안내 주석에 Linux 경로 예시(`linux-x64`)가 누락되어 있어 macOS/Windows 예시와 나란히 추가(README §환경 변수·config.py `_auto_detect_dce()` Linux 분기와 정합). 나머지 전체 클레임(코드 import 15개 전량, GUI_SPEC/GUI_TEST_CHECKLIST/CORE_API_PLAN 위젯·에러메시지·상태전이, README 기능표/다운로드표/CLI 옵션, pyproject.toml/CI 매트릭스, git 워킹트리 uncommitted 상태) MATCH — 전 회차(16차) 대비 신규 코드 커밋 없음. |
| 2026-07-07 | Doc-Sync 18차 검증 — MISMATCH 0건. 전 회차(17차, 2026-07-04) 대비 소스 커밋 없음(마지막 커밋 여전히 `24ded85`, 2026-06-05; 8개 파일 uncommitted 상태 그대로 유지: `.env.example`/`CLAUDE.md`/`PUBLICATION_REVIEW.md`/`README.md`/`gui.py`/`docs/CORE_API_PLAN.md`/`docs/GUI_SPEC.md`/`docs/GUI_TEST_CHECKLIST.md`). 이번 회차 재확인 범위: `gui.py`(2160줄) 위젯 25개·`FORMAT_OPTIONS` 6종·`_get_export_format()` 재사용(2026-06-30 수정 반영)·`_test_channels_parallel()` dead code 잔존(gui.py:758, 미호출, 코드 정리 권고 재확인)·`_validate_inputs()`/`_run_export()` 에러 문자열 전수, `utils.py`(URL 파싱 6종 에러메시지·`validate_channel_id`/`validate_date_range`/`validate_output_directory`/`validate_dce_executable`·`sanitize_error_message`·`convert_json_to_markdown`·`cleanup_avatar_emoji_files`), `exporter.py`(`_build_command`·`safe_mode`→`--parallel 1`·스트리밍/블로킹 이중 경로), `config.py`(`_auto_detect_dce` 3-OS 분기·0600 권한·env>config 우선순위), `cli.py`(서브커맨드 6종·`CLI_FORMATS`·`--include-threads` choices), `pyproject.toml`/`.github/workflows/test.yml`(CI 매트릭스: ubuntu+macos py3.9-3.13 + windows-latest py3.12 smoke), `PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md`(공개 준비 상태 문구 일치) — 전체 MATCH. |
| 2026-07-08 | Doc-Sync 19차 검증 — MISMATCH 0건. 전 회차(18차, 2026-07-07) 대비 소스 커밋 없음(마지막 커밋 여전히 `24ded85`, 2026-06-05; 동일 8개 파일 uncommitted 상태 유지, `gui.py` mtime 2026-06-30 이후 변경 없음). 이번 회차 재확인 범위: `cli.py`/`config.py`/`utils.py`/`exporter.py`/`__init__.py`/`pyproject.toml` 전량 재독, `gui.py` 2160줄 전량 재독(위젯 25개·`FORMAT_OPTIONS` 6종·`_get_export_format()` 재사용·`_test_channels_parallel()` dead code 잔존(gui.py:758, 미호출) 재확인), `setup.py`, `tests/`(5개 파일 704줄), `.github/workflows/test.yml`(CI 매트릭스 재확인), `PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md` 공개 준비 문구 재확인. 교차 검증: README "토큰이 설정되지 않았습니다"(gui.py:1328 `_on_export` 초기 자동 프롬프트 로그)와 GUI_SPEC E1 "Discord 토큰이 설정되지 않았습니다"(gui.py:1883/1535 `_validate_inputs`/`_run_export` 검증 실패 경로)가 서로 다른 코드 경로의 문자열을 각각 정확히 인용하고 있음을 확인 — 오탐 아님, 수정 불필요. `ruff check discord_exporter/ tests/` 재실행 clean, `pytest` 로컬 미설치 재확인(PUBLICATION_REVIEW.md 기재와 일치) — 전체 MATCH, 신규 UNDOCUMENTED/STALE 없음. |
| 2026-07-09 | Doc-Sync 20차 검증 — MISMATCH 1건 수정: GUI_SPEC §2 위젯 목록의 `btn_browse_output`/`btn_open_folder`/`btn_clear_log`가 `gui.py`에서 `self.` 인스턴스 속성으로 저장되지 않는 익명 `ttk.Button`으로 구현되어 있음(실제 명명된 버튼 속성은 `self.btn_export`/`self.btn_stop` 2개뿐, `grep -c "self\.btn_" gui.py` = 9건 전부 이 2개에서만 발생)을 확인하고 표에 `†` 각주로 명시(라벨·동작 자체는 일치, 속성명만 실재하지 않음 — 기능적 MATCH이나 "위젯 ID" 표기가 오도 가능하여 저심각 MISMATCH로 분류). 전 회차(19차, 2026-07-08) 대비 소스 커밋 없음(마지막 커밋 `24ded85` 유지, 8개 파일 uncommitted 그대로). 재확인 범위: CLI 서브커맨드 6종·`CLI_FORMATS`/`ExportFormat.from_string`(`md`/`markdown`/`obsidian` 별칭 일치)·`validate_channel_id`/`validate_date_range`/`validate_output_directory`/`validate_dce_executable`/`parse_date` 에러 문자열 전수 재대조(README §GUI 오류표, GUI_SPEC §5 E1-E10, GUI_TEST_CHECKLIST 시나리오 1-10과 문자 단위 일치)·`FORMAT_OPTIONS` 6종·`_test_channels_parallel` dead code 잔존 재확인(호출부 0건)·`.env.example` Linux 경로 예시·`NOTICE.md`/CI 매트릭스(ubuntu+macos py3.9-3.13+windows smoke). `ruff check discord_exporter/ tests/` clean, `pytest` 로컬 미설치 재확인. 나머지 전체 MATCH. |
| 2026-07-11 | Doc-Sync 21차 검증 — MISMATCH 1건 수정: GUI_TEST_CHECKLIST 시나리오 4가 예시 입력 3개(`12345`/`abc123456789012345678`/30자리 숫자)에 대해 단일 메시지("... should be 17-20 digits ...")만 예상 결과로 기재하고 있었으나, `utils.py:validate_channel_id()`는 `isdigit()` 체크를 길이 체크보다 먼저 수행하므로 문자가 포함된 `abc123456789012345678` 입력은 실제로는 "Channel ID must be a number, got: ..." 메시지를 발생시킴(길이 체크 도달 안 함) — `tests/test_utils.py::test_rejects_letters`(83행)가 이 분기를 별도로 검증하고 있어 코드 의도임을 확인. 시나리오 4에 케이스별 예상 메시지 구분 주석과 체크리스트 항목 추가. 20차(2026-07-09)까지 20회 연속 재확인에서 놓쳤던 항목 — 예시 입력 3개 각각을 실제 코드 분기(isdigit → 길이) 순서대로 재추적해 발견함. 전 회차(20차) 대비 소스 커밋 없음(마지막 커밋 `59206fe` 유지, git status clean, 전체 파일 mtime이 마지막 커밋 시각 이후로 변경된 파일 0건 — `find . -newermt "2026-07-09 15:01:39"` 결과 없음). 재확인 범위: `ruff check discord_exporter/ tests/` clean, `pytest` 로컬 미설치 재확인, GUI_SPEC §2/§3/§5 위젯·상태전이·에러메시지 전수 재대조(E1-E10 모두 MATCH), README 기능표/다운로드표/CLI옵션/보안 섹션 재확인, `utils.py`/`config.py`/`exporter.py`/`cli.py` 전량 재독(safe_mode 이중 기본값 dataclass=False/GUI=True, `<TOKEN>`/`******` 이중 마스킹, `_auto_detect_dce()` 3-OS 분기, `validate_channel_id`/`validate_dce_executable`/`validate_output_directory`/`parse_date`/`validate_date_range` 에러 문자열 전수), `pyproject.toml`/`.github/workflows/test.yml`/`.env.example`/`PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md`/`docs/CORE_API_PLAN.md` 재확인 — 나머지 전체 MATCH. |
| 2026-07-14 | Doc-Sync 22차 검증 — MISMATCH 0건. 전 회차(21차, 2026-07-11) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, 소스 파일 mtime 전량 06-02~07-11 범위에서 불변, 21차의 CLAUDE.md·GUI_TEST_CHECKLIST.md 수정분은 여전히 uncommitted 상태로 working tree에 유지됨 — `git status` 확인). 이번 회차는 독립 재검증으로 수행(과거 22회 기록 신뢰 대신 코드 재대조): `gui.py`(2159줄) `grep -c "self\.btn_"` = 9건 전부 `btn_export`/`btn_stop`에서만 발생(20차 각주 재확인), `_test_channels_parallel`(758행) 여전히 정의만 있고 호출부 0건(dead code 잔존), `FORMAT_OPTIONS` 6종 및 `_get_export_format()` 재사용 구조 유지, `lbl_export_target` 초기값 `"URL을 입력하세요"`(357행) 확인, `safe_mode`/`exclude_avatars`/`exclude_emojis` GUI 기본값(True/True/True) 및 dataclass 기본값(safe_mode=False) 이중 기본값 재확인, `_validate_inputs()` E1/E4/E6 메시지 문자 단위 재대조(1883/1899-1900/1916행) — 전부 GUI_SPEC/README/GUI_TEST_CHECKLIST 기재와 MATCH. `cli.py`(CLI_FORMATS 8종·서브커맨드 6종·`--include-threads` choices, exportdm의 `include_threads=None` 강제 재확인), `utils.py`(`ExportFormat.from_string` 별칭 15종, `validate_channel_id`/`validate_guild_id`/`parse_date`/`validate_date_range`/`validate_output_directory`/`validate_dce_executable`/`sanitize_error_message` 에러 문자열 전수), `config.py`(`_auto_detect_dce()` 3-OS 분기, 0600 권한, env>config 우선순위, `mask_token()` 포맷), `exporter.py`(`_build_command`·`_build_display_command`(`******`)·`_run_command`(`<TOKEN>`) 이중 마스킹, safe_mode→`--parallel 1`) 전량 재독 — 전체 MATCH. `tests/`(6개 파일 704줄, 구성 동일), `ruff check discord_exporter/ tests/` clean(신규 실행 확인), `pytest` 로컬 미설치 재확인(PUBLICATION_REVIEW.md 기재와 일치). `pyproject.toml`/`.github/workflows/test.yml`(ubuntu+macos py3.9-3.13 매트릭스 + windows-latest py3.12 smoke)/`.env.example`(3-OS 경로 예시)/`PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md`/`docs/CORE_API_PLAN.md` 재확인 — 신규 UNDOCUMENTED/STALE 없음. |
| 2026-07-15 | Doc-Sync 23차 검증 — MISMATCH 0건. 전 회차(22차, 2026-07-14) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, `find . -newermt <커밋시각>` 결과 `CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md` 2개뿐 — 21차 수정분이 여전히 uncommitted, 그 외 전 파일 mtime 불변). 독립 재검증 범위: `gui.py`(2159줄, 1-1664행+1871-1938행+2063-2159행 직접 재독) `_validate_inputs()` E1/E4/E6 메시지(1883/1899-1900/1916행) 및 `TokenDialog`(`Entry(show="*")`, 2100행) 문자 단위 재확인, `FORMAT_OPTIONS` 6종·`_get_export_format()` 재사용(1479행)·`_test_channels_parallel` dead code(758행, 호출부 0건) 재확인; `utils.py`(1195줄 전량) `validate_channel_id`(길이 검사 전에 `isdigit()` 우선, 21차 각주와 일치)·`sanitize_error_message`·`convert_json_to_markdown`·`cleanup_avatar_emoji_files` 전량 재대조; `cli.py`(539줄)·`config.py`(294줄, `_auto_detect_dce()` 3-OS 분기·0600 권한)·`exporter.py`(767줄, 이중 마스킹·safe_mode→`--parallel 1`)·`__init__.py` 전량 재독 — 전체 MATCH. `README.md`/`docs/GUI_SPEC.md`/`docs/GUI_TEST_CHECKLIST.md`/`docs/CORE_API_PLAN.md`/`PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md`/`pyproject.toml`/`.github/workflows/test.yml`/`.env.example`/`setup.py` 전량 재독 — 전체 MATCH. `ruff check discord_exporter/ tests/` 신규 실행 clean, `pytest` 로컬 미설치 재확인(PUBLICATION_REVIEW.md 기재와 일치). `tests/`(6개 파일 704줄 합산 재계산 일치, `test_rejects_letters`는 81행 — 21차 로그의 "83행"은 그 시점 스냅샷 기록이며 파일 자체 변경 없음, 과거 로그 항목은 정정하지 않음). 신규 UNDOCUMENTED/STALE 없음. |
| 2026-07-16 | Doc-Sync 24차 검증 — MISMATCH 0건. 전 회차(23차, 2026-07-15) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, `git diff --stat 59206fe -- discord_exporter/ tests/ pyproject.toml .github/ setup.py` 결과 0건 — 21차의 `CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md` 수정분만 여전히 uncommitted). 독립 재검증 범위: `gui.py`(`grep -n "self\.btn_"` 9건 전부 `btn_export`/`btn_stop`에서만 발생 재확인, `_test_channels_parallel`(758행) 호출부 0건 재확인, `FORMAT_OPTIONS` 6종(55-62행), `lbl_export_target`(357행), `safe_mode` GUI 기본값 True(89행) 재확인, `_validate_inputs()` E1(1883행)/E4(1899-1900행)/E6(1916행) 문자 단위 재대조), `cli.py`(`CLI_FORMATS`(331행)·서브커맨드 6종(437-517행)·`exportdm`의 `include_threads=None` 강제(249행) 재확인), `utils.py`(`validate_channel_id`(326-346행, `isdigit()` 우선순위 21차 각주와 일치)·`validate_dce_executable`(587-602행, "DCE 경로가 설정되지 않았습니다." 재확인)·`validate_output_directory`(531-544행)·`validate_date_range`(492행) 재독), `.env.example`(3-OS 경로 예시 유지)·`.github/workflows/test.yml`(ubuntu+macos py3.9-3.13 매트릭스 + windows-latest py3.12 smoke 재확인), `NOTICE.md`/`PUBLICATION_REVIEW.md`(공개 준비 상태 `ready-for-final-visibility-approval` 재확인) 신규 재독 — 전체 MATCH. `ruff check discord_exporter/ tests/` 신규 실행 clean, `pytest` 로컬 미설치 재확인. 신규 UNDOCUMENTED/STALE 없음. |
| 2026-07-17 | Doc-Sync 25차 검증 — MISMATCH 0건. 전 회차(24차, 2026-07-16) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe`, 2026-07-09 유지, `git diff --stat 59206fe -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `find . -newermt <커밋시각>` 결과 `CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md` 2개뿐 — 21차 수정분이 여전히 uncommitted). 독립 재검증 범위: `gui.py`(`grep -n "self\.btn_"` 9건 전부 `btn_export`/`btn_stop`에서만 발생 재확인, `_test_channels_parallel`(758행) 호출부 0건 재확인, `FORMAT_OPTIONS` 6종(55-62행), `lbl_export_target` 초기값 `"URL을 입력하세요"`(357행) 재확인, `safe_mode` GUI 기본값 True(89행), `_validate_inputs()` E1(1883행)/E4(1899-1900행)/E6(1916행) 문자 단위 재대조 — GUI_SPEC §2/§5, GUI_TEST_CHECKLIST 시나리오 1-10과 MATCH), `cli.py`(`CLI_FORMATS` 8종(331-335행)·subparser 7개(setup/export/exportguild/exportdm/list-guilds/list-channels/list-dm, 437-524행)와 README CLI 옵션표 MATCH), `utils.py`(`validate_channel_id()`(326-346행, `isdigit()` 우선순위 GUI_TEST_CHECKLIST 시나리오 4 각주와 일치)·`validate_dce_executable()`(587-602행, "DCE 경로가 설정되지 않았습니다." GUI_SPEC E2와 일치) 재독), `pyproject.toml`(`[project.scripts]`/`[project.gui-scripts]` 엔트리포인트 GUI_SPEC §7과 일치)·`.github/workflows/test.yml`(ubuntu+macos py3.9-3.13 매트릭스 + windows-latest py3.12 smoke, README "CI는 Linux+macOS 매트릭스…Windows smoke" 서술과 일치)·`.env.example`(3-OS 경로 예시 유지), `PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md`(레포명 `nori00000/discord-downloader` — `git remote -v`와 일치, 공개 준비 상태 `ready-for-final-visibility-approval` 재확인) 신규 재독 — 전체 MATCH. `ruff check discord_exporter/ tests/` 신규 실행 clean(exit 0), `pytest` 로컬 미설치 재확인(exit 1, `No module named pytest`). 신규 UNDOCUMENTED/STALE 없음. |
| 2026-07-19 | Doc-Sync 26차 검증 — MISMATCH 0건. 전 회차(25차, 2026-07-17) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, `git diff --stat 59206fe -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `find . -newermt "2026-07-09 15:01:39"` 결과 `CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md` 2개뿐 — 21차 수정분이 8일째 uncommitted 유지). 독립 재검증 범위: `gui.py`(2159줄, `grep -n "self\.btn_"` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생 재확인, `_test_channels_parallel`(758행) 정의만 존재·호출부 0건 재확인, `FORMAT_OPTIONS` 6종(55-62행), `lbl_export_target`(357행)·`safe_mode` GUI 기본값 True(89행)·`exclude_avatars`/`exclude_emojis` True(86-87행)·`filter_text_only` True/`filter_accessible` False(275·282행)·`cmb_threads` 기본 "all"(370-378행) 재확인), `cli.py`(539줄, subparser 7개(437-524행) README CLI 옵션표와 MATCH, `exportdm`의 `include_threads=None` 강제(247-249행) 재확인), `utils.py`(1194줄, `validate_channel_id()`(326-351행, `isdigit()`가 길이 체크보다 우선 — GUI_TEST_CHECKLIST 시나리오 4 각주와 일치)·`parse_date`/`validate_date_range`/`validate_output_directory`/`validate_dce_executable`/`sanitize_error_message` 정의 위치 전수 재확인), `pyproject.toml`(엔트리포인트·CI 매트릭스 선언·ruff 설정)·`.github/workflows/test.yml`(ubuntu+macos py3.9-3.13 매트릭스 + windows-latest py3.12 smoke 잡, README 서술과 일치)·`.env.example`(3-OS 경로 예시 유지)·`setup.py`(레거시 호환용, 내용 변경 없음), `PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md`(공개 준비 상태 `ready-for-final-visibility-approval` 재확인) 재독 — 전체 MATCH. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!), `pytest` 로컬 미설치 재확인(exit 1, `No module named pytest`, PUBLICATION_REVIEW.md 기재와 일치). 신규 UNDOCUMENTED/STALE 없음. **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분(`CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md`)이 8일 연속 uncommitted 상태 — 문서-코드 정합성에는 영향 없으나 커밋 정리는 별도 판단 필요(자동 커밋하지 않음, 사용자 요청 시에만). |
| 2026-07-20 | Doc-Sync 27차 검증 — MISMATCH 0건. 전 회차(26차, 2026-07-19) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, `git diff --stat 59206fe -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `find . -newermt "2026-07-09T15:01:39+09:00"` 결과 `CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md` 2개뿐 — 21차 수정분이 9일째 uncommitted 유지). 독립 재검증 범위: `gui.py`(2159줄, `grep -n "self\.btn_"` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생 재확인, `_test_channels_parallel`(758행) 정의만 존재·호출부 0건 재확인, `FORMAT_OPTIONS` 6종(55-62행: HTML Dark/Light·Markdown(Obsidian)·Plain Text·JSON·CSV)·`safe_mode`/`exclude_avatars`/`exclude_emojis` GUI 기본값 True(86-89행) 재확인), `cli.py`(539줄, `CLI_FORMATS` 8종(331-335행)이 README §출력 형식 8행과 MATCH, subparser 7개(437-524행), `exportdm`의 `include_threads=None` 강제(247-249행) 재확인), `utils.py`(1194줄, `validate_channel_id()`(326-351행, `isdigit()`가 길이 체크보다 우선 — GUI_TEST_CHECKLIST 시나리오 4 각주와 일치)·`validate_dce_executable()`(587-602행, "DCE 경로가 설정되지 않았습니다." GUI_SPEC E2와 일치)·`ExportFormat.from_string()`에 `'markdown'` 별칭(122행) 존재 확인 — README "md/markdown" 표기와 CLI_FORMATS 리스트에는 'markdown' 리터럴이 없다는 점이 서로 다른 계층(별칭 vs `--format` 도움말 리스트)의 사실임을 재확인, 오탐 아님), `exporter.py`(`safe_mode: bool = False` dataclass 기본값(41행) — GUI True 기본값과의 의도적 이중 기본값이 CORE_API_PLAN §3.1 서술과 일치) 재독 — 전체 MATCH. `docs/GUI_SPEC.md`(위젯 26행·상태전이·E1-E10 에러표)·`docs/GUI_TEST_CHECKLIST.md`(시나리오 1-10)·`docs/CORE_API_PLAN.md`(설계-구현 차이 각주 3건 유지)·`README.md`(기능표·다운로드표·CLI 옵션·보안 섹션)·`PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md`(레포명 `nori00000/discord-downloader` `git remote -v`와 일치, 공개 준비 상태 `ready-for-final-visibility-approval` 재확인)·`.env.example`(3-OS 경로 예시)·`.github/workflows/test.yml`(ubuntu+macos py3.9-3.13 매트릭스 + windows-latest py3.12 smoke)·`setup.py`(레거시 호환, 변경 없음)·`tests/`(6개 파일 704줄 합산 재확인, `test_rejects_letters` 81행 재확인) 전량 재독 — 전체 MATCH, 신규 UNDOCUMENTED/STALE 없음. `ruff check discord_exporter/ tests/`(경로: `/opt/homebrew/bin/ruff`, `python3 -m ruff`는 3.14 환경에 미설치라 별도 확인 필요했음) 신규 실행 clean(All checks passed!), `pytest` 로컬 미설치 재확인(`which pytest` 없음, PUBLICATION_REVIEW.md 기재와 일치). **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분(`CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md`)이 9일 연속 uncommitted 상태 유지 — 문서-코드 정합성에는 영향 없으므로 자동 커밋하지 않음(사용자 요청 시에만). |
| 2026-07-27 | Doc-Sync 28차 검증 — MISMATCH 0건. 전 회차(27차, 2026-07-20) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, `git log -1 --format=%cI` = `2026-07-09T15:01:39+09:00`, `find . -newermt <커밋시각>` 결과 `CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md` 2개뿐 — 21차 수정분이 18일째 uncommitted 유지, 이번 회차는 전 회차 대비 7일 공백 — 그 사이 신규 커밋 없음). 독립 재검증 범위(전체 소스·문서 재독): `gui.py`(2159줄 전량, `grep -n "self\.btn_"` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생 재확인, `_test_channels_parallel`(758행) 정의만 존재·호출부 0건 재확인, `FORMAT_OPTIONS` 6종(55-62행), `exclude_avatars`/`exclude_emojis`/`include_threads`/`safe_mode` GUI 기본값(86-89행: True/True/"all"/True), `lbl_export_target` 초기값 `"URL을 입력하세요"`(357행), `_validate_inputs()` E1(1883행)/E4(1899-1900행)/E6(1916행) 메시지 문자 단위 재대조, `TokenDialog.ent_token`(`Entry(show="*")`, 2100행)); `utils.py`(1194줄 전량, `validate_channel_id()`(326-351행, `isdigit()` 우선순위 GUI_TEST_CHECKLIST 시나리오 4 각주와 일치)·`validate_dce_executable()`(601-602행 "DCE 경로가 설정되지 않았습니다.")·`ExportFormat.from_string()` 별칭 15종(107-124행, `'markdown'` 122행 포함)); `cli.py`(539줄 전량, `CLI_FORMATS` 8종(331-335행)·subparser 7개(437-524행)·`exportdm`의 `include_threads=None` 강제(247-249행)); `config.py`(294줄 전량, `_auto_detect_dce()` 3-OS 분기(183-216행)·0600 권한(`_restrict_permissions`)·env>config 우선순위·`mask_token()` 포맷); `exporter.py`(767줄 전량, `safe_mode: bool = False` dataclass 기본값(41행)·`_build_display_command()`(`******`)/`_run_command()`(`<TOKEN>`) 이중 마스킹·`--parallel 1`) — 전체 MATCH. `docs/GUI_SPEC.md`(위젯 26행·상태전이·E1-E10)·`docs/GUI_TEST_CHECKLIST.md`(시나리오 1-10, 시나리오 4 각주 포함)·`docs/CORE_API_PLAN.md`(설계-구현 차이 각주 3건)·`README.md`(기능표·다운로드표(Linux 포함)·CLI 옵션·출력 형식 8행·보안 섹션)·`PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md`(레포명 `nori00000/discord-downloader` `git remote -v`와 일치, 공개 준비 상태 `ready-for-final-visibility-approval` 재확인)·`.env.example`(3-OS 경로 예시)·`pyproject.toml`(엔트리포인트·CI 매트릭스·ruff 설정)·`.github/workflows/test.yml`(ubuntu+macos py3.9-3.13 매트릭스 + windows-latest py3.12 smoke)·`tests/`(6개 파일, `wc -l` 재계산: conftest 39·test_cli 111·test_config 95·test_exporter 194·test_utils 265 = 704줄, `__init__.py` 0줄 별도 — 27차까지의 "704줄" 표기와 일치) 전량 재독 — 전체 MATCH, 신규 UNDOCUMENTED/STALE 없음. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `pytest`/`python3 -m pytest` 로컬 미설치 재확인(`No module named pytest`, PUBLICATION_REVIEW.md 기재와 일치). **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분(`CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md`)이 18일 연속 uncommitted 상태 유지 — 문서-코드 정합성에는 영향 없으므로 자동 커밋하지 않음(사용자 요청 시에만). |
| 2026-07-28 | Doc-Sync 29차 검증 — MISMATCH 0건. 전 회차(28차, 2026-07-27) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, `git log -1 --format=%cI` = `2026-07-09T15:01:39+09:00`, `git diff --stat 59206fe -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `find . -newermt <커밋시각>` 결과 `CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md` 2개뿐 — 21차 수정분이 19일째 uncommitted 유지). 독립 재검증 범위: `gui.py`(2159줄, `grep -n "self\.btn_"` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생 재확인, `_test_channels_parallel`(758행) 정의만 존재·호출부 0건 재확인, `FORMAT_OPTIONS`(55행)·`exclude_avatars`/`exclude_emojis`/`safe_mode` GUI 기본값 True(86-89행)·`filter_text_only`=True/`filter_accessible`=False(275·282행)·`lbl_export_target` 초기값(357행)·`_validate_inputs()` E1(1883행)/E4(1899-1900행)/E6(1916행) 재대조), `utils.py`(`validate_channel_id()`(326-351행, `isdigit()` 우선순위)·`validate_dce_executable()`(601-602행)·`validate_date_range()`(522행 "늦거나 같습니다")·`validate_output_directory()`(567행 "폴더에 쓰기 권한이 없습니다")·`ExportFormat.from_string()`(92-112행) 재확인), `cli.py`(`CLI_FORMATS` 8종(331-335행: html/html-dark/html-light/txt/json/csv/md/obsidian, README 출력 형식 표와 MATCH)·subparser 7개·`exportdm`의 `include_threads=None` 강제(249행) 재확인), `config.py`(`_auto_detect_dce()` 3-OS 분기(183-216행) 재확인), `exporter.py`(`safe_mode: bool = False` dataclass 기본값(41행) 재확인) — 전체 MATCH. `docs/GUI_SPEC.md`·`docs/GUI_TEST_CHECKLIST.md`·`docs/CORE_API_PLAN.md`·`README.md`·`PROJECT.md`·`NOTICE.md`·`PUBLICATION_REVIEW.md`·`.env.example`·`pyproject.toml`·`.github/workflows/test.yml`·`setup.py` 전량 재독 — 전체 MATCH, 신규 UNDOCUMENTED/STALE 없음. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`, PUBLICATION_REVIEW.md 기재와 일치). **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분(`CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md`)이 19일 연속 uncommitted 상태 유지 — 문서-코드 정합성에는 영향 없으므로 자동 커밋하지 않음(사용자 요청 시에만). |
| 2026-08-01 | Doc-Sync 30차 검증 — MISMATCH 0건. 전 회차(29차, 2026-07-28) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, `git log -1 --format=%cI` = `2026-07-09T15:01:39+09:00`, `git diff --stat 59206fe -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `find . -newermt <커밋시각>` 결과 `CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md` 2개뿐 — 21차 수정분이 21일째 uncommitted 유지). 독립 재검증 범위: `gui.py`(2159줄, `grep -n "self\.btn_"` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생 재확인, `_test_channels_parallel`(758행) 정의만 존재·호출부 0건 재확인, `FORMAT_OPTIONS` 6종(55-62행) 재확인, `validate_channel_id()`(326-351행, `isdigit()` 우선순위 GUI_TEST_CHECKLIST 시나리오 4 각주와 일치) 재확인) — 전체 MATCH. `git remote -v`(`nori00000/discord-downloader`)가 `PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md` 레포명 표기와 일치 재확인, `PUBLICATION_REVIEW.md` 전문 재독(공개 준비 상태 `ready-for-final-visibility-approval`, pytest 미설치 기재 유지) 재확인, `.github/workflows/test.yml` 매트릭스(`ubuntu-latest`/`macos-latest` × py3.9-3.13) 재확인, `pyproject.toml`(`[project.scripts]`/`[project.gui-scripts]` 엔트리포인트) 재확인. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`, PUBLICATION_REVIEW.md 기재와 일치). 신규 UNDOCUMENTED/STALE 없음. **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분(`CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md`)이 21일 연속 uncommitted 상태 유지 — 문서-코드 정합성에는 영향 없으므로 자동 커밋하지 않음(사용자 요청 시에만). |
| 2026-08-05 | Doc-Sync 31차 검증 — **MISMATCH 1건 수정(30회 연속 MATCH 이후 최초 신규 발견)**: `docs/GUI_SPEC.md` §5 에러 시나리오 표의 E10 표시 메시지 `"Discord 접근 실패. 토큰을 확인하세요"`가 코드 어디에도 리터럴로 존재하지 않음을 확인(`grep -rn "접근 실패\|토큰을 확인하세요" discord_exporter/` = `utils.py:695` `'인증 오류: Discord 토큰을 확인하세요.\n'` 1건뿐, "접근 실패"라는 문구 자체가 코드에 없음). 실제 동작 추적: `gui.py:_on_export_error`(1807-1817행)가 E9/E10 공통으로 다이얼로그 제목 "내보내기 실패" + 본문 `f"{error}\n\n해결 방법:\n{solution}"`을 표시하며, `error`는 `sanitize_error_message()` 처리 후 `parse_dce_error()`(utils.py:666-789)가 매칭된 에러 키워드에 따라 반환하는 여러 한국어 번역 중 하나(예: `unauthorized`→"인증 실패: Discord 토큰이 유효하지 않습니다.", `authentication`→"인증 오류: Discord 토큰을 확인하세요.", `connection`→"연결 오류: Discord 서버에 연결할 수 없습니다." 등), `solution`은 `get_error_solution()`(utils.py:791-812) 반환값. E9/E10이 실제로는 동일한 UI 메커니즘(같은 다이얼로그)을 공유하며 카테고리만 다르다는 사실이 기존 표에 반영되어 있지 않았음. E9/E10 행을 실제 코드 경로 인용으로 교정. 다른 문서(README.md/docs/CORE_API_PLAN.md/docs/GUI_TEST_CHECKLIST.md)에는 해당 고정 문자열 참조가 없어 파급 범위는 `docs/GUI_SPEC.md` 1개 파일로 한정됨. 전 회차(30차, 2026-08-01) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, `git diff --stat 59206fe -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `find . -newermt <커밋시각>` 결과 `CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md` 2개뿐 — 21차 수정분이 25일째 uncommitted 유지). 독립 재검증 범위: `FORMAT_OPTIONS`(55-62행)·`self.btn_` 9건(btn_export/btn_stop 전용)·`_test_channels_parallel`(758행, 호출부 0건)·`CLI_FORMATS`(331-335행)·subparser 7개(437-524행)·`validate_channel_id()`(326-351행)·`validate_dce_executable()` E2 메시지(602행)·`safe_mode` 이중 기본값(exporter.py:41=False, gui.py:89=True)·`lbl_export_target` 초기값/전이 텍스트(359행/584행) 재확인 — 전부 MATCH. `.env.example`(3-OS 경로 예시)·`.github/workflows/test.yml`(ubuntu+macos py3.9-3.13 매트릭스+windows smoke)·`git remote -v`(`nori00000/discord-downloader`, `PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md`와 일치)·`PUBLICATION_REVIEW.md`(공개 준비 상태 유지) 재확인 — MATCH. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`). **관찰(수정 불필요)**: 21차(2026-07-11) 수정분이 25일 연속 uncommitted 유지. |
| 2026-08-06 | Doc-Sync 32차 검증 — MISMATCH 0건. 전 회차(31차, 2026-08-05) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, `git log -1 --format=%cI` = `2026-07-09T15:01:39+09:00`, `git diff --stat 59206fe -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `find . -newermt <커밋시각>` 결과 `CLAUDE.md`·`docs/GUI_SPEC.md`·`docs/GUI_TEST_CHECKLIST.md` 3개뿐 — 21차+31차 수정분이 각각 26일째/1일째 uncommitted 유지). 독립 재검증 범위(코드 전량 재독 후 문서와 대조, 과거 로그 신뢰하지 않고 재추적): `gui.py`(2159줄, `grep -n "self\.btn_"` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생 재확인, `_test_channels_parallel`(758행) 호출부 0건 재확인 — `grep -n "_test_channels_parallel("` 결과 정의 1건뿐, dead code 잔존, 코드 수정 권고 유지·문서 수정 불필요, `FORMAT_OPTIONS` 6종(55-62행) 재확인, `exclude_avatars`(86행)/`exclude_emojis`(87행)/`include_threads`(88행, 기본값 "all")/`safe_mode`(89행, 기본값 True) 재확인, `filter_text_only`(275행 True)/`filter_accessible`(282행 False) 재확인, `lbl_export_target` 초기값 `"URL을 입력하세요"`(359행) 재확인, `cmb_threads.current(2)`("all" 기본, 378행) 재확인, `TokenDialog.ent_token`(`show="*"`, 2100행) 재확인, `_validate_inputs()` E1(1883행 "Discord 토큰이 설정되지 않았습니다")/E4(1898-1901행 "내보내기 대상이 없습니다.\nDiscord URL을 입력하거나 채널을 체크해주세요.")/E6(1916행 "채널 ID 오류: {e}") 문자 단위 재대조, `_on_export_error()`(1807-1817행, 다이얼로그 제목 "내보내기 실패" + `f"{error}\n\n해결 방법:\n{solution}"`)가 31차 수정 후 GUI_SPEC E9/E10 서술과 여전히 일치함을 재확인) — 전체 MATCH. `utils.py`(1194줄 전량, `validate_channel_id()`(326-351행, `isdigit()`가 길이 체크보다 우선 — GUI_TEST_CHECKLIST 시나리오 4 각주와 일치)·`validate_dce_executable()`(601-602행 "DCE 경로가 설정되지 않았습니다.")·`validate_output_directory()`(566-572행 "폴더에 쓰기 권한이 없습니다: {path}")·`validate_date_range()`(521-526행 "시작일이 종료일보다 늦거나 같습니다")·`parse_date()`(486-489행)·`ExportFormat.from_string()` 별칭 15종(107-124행)·`parse_dce_error()`/`get_error_solution()`(666-816행) 전량 재독) — MATCH. `cli.py`(539줄 전량, `CLI_FORMATS` 8종(331-335행)·subparser 7개(setup/export/exportguild/exportdm/list-guilds/list-channels/list-dm, 437-524행)·`exportdm`의 `include_threads=None` 강제(247-249행)) — MATCH. `config.py`(294줄 전량, `_auto_detect_dce()` 3-OS 분기(183-216행)·`_restrict_permissions()` 0600(108-123행)·`get_token()`/`get_dce_path()` env>config>자동탐지 우선순위·`mask_token()` 포맷(237-249행)) — MATCH. `exporter.py`(767줄 전량, `ExportOptions.safe_mode: bool = False` 기본값(41행, GUI True와의 의도적 이중 기본값)·`_build_display_command()`(`******`, 219-242행)/`_run_command()`(`<TOKEN>`, 389-453행) 이중 마스킹·`--parallel 1`(213-215행)) — MATCH. `__init__.py`(버전 1.1.0 재확인)·`setup.py`(레거시 호환, 변경 없음)·`tests/`(6개 파일, `wc -l` 재계산: conftest 39·test_cli 111·test_config 95·test_exporter 194·test_utils 265 = 704줄, 과거 로그 기재와 일치) — MATCH. `docs/GUI_SPEC.md`(위젯 26행·상태전이·E1-E10, 31차 정정분 포함 재확인)·`docs/GUI_TEST_CHECKLIST.md`(시나리오 1-10, 21차 각주 포함)·`docs/CORE_API_PLAN.md`(설계-구현 차이 각주 3건)·`README.md`(기능표·다운로드표(Linux 포함)·CLI 옵션·출력 형식 8행·GUI 오류표·보안 섹션)·`PROJECT.md`·`NOTICE.md`·`PUBLICATION_REVIEW.md`(레포명 `nori00000/discord-downloader`, `git remote -v`와 일치 재확인, 공개 준비 상태 `ready-for-final-visibility-approval` 재확인)·`.env.example`(3-OS 경로 예시)·`pyproject.toml`(엔트리포인트·CI 매트릭스 선언·ruff 설정)·`.github/workflows/test.yml`(ubuntu+macos py3.9-3.13 매트릭스 + windows-latest py3.12 smoke) 전량 재독 — 전체 MATCH, 신규 UNDOCUMENTED/STALE 없음. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`, PUBLICATION_REVIEW.md 기재와 일치). `AGENTS.md`/`.cursor/rules`/`*.rule` 등 추가 규칙 문서는 레포에 존재하지 않음(`find` 확인) — 해당 없음. **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분(`CLAUDE.md`·`docs/GUI_TEST_CHECKLIST.md`)이 26일 연속, 31차(2026-08-05) 수정분(`CLAUDE.md`·`docs/GUI_SPEC.md`)이 1일째 uncommitted 유지 — 문서-코드 정합성에는 영향 없으므로 자동 커밋하지 않음(사용자 요청 시에만). |
| 2026-08-10 | Doc-Sync 33차 검증 — MISMATCH 0건. 전 회차(32차, 2026-08-06) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, `git log -1 --format="%H %cI"` = `59206fe1... 2026-07-09T15:01:39+09:00`, `find . -newermt "2026-07-09T15:01:39+09:00" -not -path "./.git/*" -not -path "./.ruff_cache/*" -not -path "./.omc/*" -type f` 결과 `CLAUDE.md`·`docs/GUI_SPEC.md`·`docs/GUI_TEST_CHECKLIST.md` 3개뿐 — 21차(2026-07-11)+31차(2026-08-05) 수정분이 각각 30일째/5일째 uncommitted 유지, `discord_exporter/`·`tests/`·`pyproject.toml`·`.github/`·`setup.py`·`.env.example` 전량 불변). 독립 재검증 범위(과거 32회 로그 신뢰 대신 코드·문서 직접 재대조): `gui.py`(`grep -n "self\.btn_"` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생 재확인 — GUI_SPEC 20차 각주와 일치, `_test_channels_parallel`(758행) 정의만 존재·호출부 0건 재확인 — dead code 잔존 관찰 유지, `FORMAT_OPTIONS` 6종(55-62행) 재확인, `_on_export_error()`(1807-1817행, 다이얼로그 제목 "내보내기 실패" + `f"{error}\n\n해결 방법:\n{solution}"`)가 31차 정정 후 GUI_SPEC E9/E10 서술과 여전히 일치함을 재확인), `utils.py`(`validate_channel_id()`(326-351행, `isdigit()`가 길이 체크보다 우선 — GUI_TEST_CHECKLIST 시나리오 4 각주와 일치) 재확인), `cli.py`(`CLI_FORMATS`(331행)·`pyproject.toml` `[project.scripts]`/`[project.gui-scripts]` 엔트리포인트 재확인), `git remote -v`(`nori00000/discord-downloader`)가 `NOTICE.md`(`nori00000` 어트리뷰션 표기) 및 `PUBLICATION_REVIEW.md` 레포명 표기와 일치, `PUBLICATION_REVIEW.md`(공개 준비 상태 `ready-for-final-visibility-approval`, `pytest not run because pytest is not installed locally` 기재 재확인), `__init__.py`(`__version__ = "1.1.0"` 재확인), `tests/` `wc -l` 재계산(conftest 39·test_cli 111·test_config 95·test_exporter 194·test_utils 265 = 704줄, `__init__.py` 0줄 별도 — 과거 로그 기재와 일치) — 전체 MATCH. 현재 working tree의 uncommitted 상태(`docs/GUI_SPEC.md` E9/E10 정정, `docs/GUI_TEST_CHECKLIST.md` 시나리오 4 케이스 분리)는 21차·31차에서 이미 코드 근거로 검증·반영된 내용 그대로이며 이번 회차 재확인에서도 코드와 문자 단위 일치 — 추가 수정 불필요. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`). 신규 UNDOCUMENTED/STALE 없음. **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분이 30일 연속, 31차(2026-08-05) 수정분이 5일째 uncommitted 유지 — 문서-코드 정합성에는 영향 없으므로 자동 커밋하지 않음(사용자 요청 시에만, `git commit` 실행은 이번 태스크 범위 밖). |
| 2026-08-11 | Doc-Sync 34차 검증 — MISMATCH 0건. 전 회차(33차, 2026-08-10) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, `git log -3 --format="%H %cI %s"`로 재확인, `git diff --stat HEAD -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `git status --short --branch`에는 `CLAUDE.md`·`docs/GUI_SPEC.md`·`docs/GUI_TEST_CHECKLIST.md` 3개만 modified — 21차(2026-07-11)+31차(2026-08-05) 수정분이 각각 31일째/6일째 uncommitted 유지). 독립 재검증 범위(과거 33회 로그 신뢰 대신 `discord_exporter/` 전 파일(`gui.py` 2160줄, `utils.py` 1195줄, `cli.py` 540줄, `config.py` 295줄, `exporter.py` 768줄) 전량 재독 후 문서와 직접 대조): `gui.py` `FORMAT_OPTIONS` 6종(55-62행)·GUI 기본값(`exclude_avatars`/`exclude_emojis`/`safe_mode`=True(86-89행), `filter_text_only`=True/`filter_accessible`=False(275·282행), `cmb_threads.current(2)`="all"(378행))·`lbl_export_target` 초기값 `"URL을 입력하세요"`(359행)/전이 텍스트 `"URL 또는 ID를 입력하세요"`(584행)·`_validate_inputs()` E1(1883행)/E4(1898-1901행)/E6(1916행)·`_on_export_error()`(1807-1817행, "내보내기 실패" + `f"{error}\n\n해결 방법:\n{solution}"`, 31차 정정 후 GUI_SPEC E9/E10과 일치)·`_test_channels_parallel`(758행, 호출부 0건 dead code 잔존)·`self.btn_` 9건(`btn_export`/`btn_stop`(1024·1032행)에서만 발생, GUI_SPEC †각주와 일치)·`TokenDialog.ent_token`(`show="*"`, 2100행) 재확인; `utils.py` `validate_channel_id()`(326-351행, `isdigit()` 우선순위)·`validate_dce_executable()`(601-602행)·`validate_output_directory()`(566-572행 "폴더에 쓰기 권한이 없습니다: {path}")·`validate_date_range()`(521-526행 "늦거나 같습니다")·`parse_date()`(486-489행)·`ExportFormat.from_string()` 별칭(107-124행, `'markdown'`/`'obsidian'` 포함)·`sanitize_error_message()` 토큰 정규식(656-663행)·`parse_dce_error()`/`get_error_solution()`(666-816행) 재확인; `cli.py` `CLI_FORMATS` 8종(331-335행, README 출력 형식 표와 MATCH)·subparser 7개(437-524행)·`exportdm`의 `include_threads=None` 강제(247-249행) 재확인; `config.py` `_auto_detect_dce()` 3-OS 분기(183-216행, Linux `linux-x64` 포함)·`_restrict_permissions()` 0600(108-123행)·env>config>자동탐지 우선순위·`mask_token()`(237-249행) 재확인; `exporter.py` `ExportOptions.safe_mode: bool = False`(41행, GUI True와 의도적 이중 기본값)·`_build_display_command()`(`******`, 219-242행)/`_run_command()`(`<TOKEN>`, 389-453행) 이중 마스킹·`--parallel 1`(213-215행) 재확인 — 전체 MATCH. `docs/GUI_SPEC.md`(위젯 26행·상태전이·E1-E10, 31차 E9/E10 정정분 포함)·`docs/GUI_TEST_CHECKLIST.md`(시나리오 1-10, 21차 시나리오 4 각주 포함)·`docs/CORE_API_PLAN.md`(설계-구현 차이 각주 3건)·`README.md`(기능표·다운로드표(Linux 포함)·설치/설정·GUI 사용법·CLI 옵션·출력 형식 8행·GUI 오류표·보안 섹션·개발/테스트)·`PROJECT.md`·`NOTICE.md`·`PUBLICATION_REVIEW.md`(레포명 `nori00000/discord-downloader`, `git remote -v`와 일치, 공개 준비 상태 `ready-for-final-visibility-approval`)·`.env.example`(3-OS 경로 예시)·`pyproject.toml`(버전 1.1.0, 엔트리포인트, CI 매트릭스 선언, ruff 설정, `dependencies = []`)·`.github/workflows/test.yml`(ubuntu+macos py3.9-3.13 매트릭스 + windows-latest py3.12 smoke, README CI 서술과 일치)·`__init__.py`(`__version__ = "1.1.0"` — pyproject.toml과 일치)·`setup.py`(레거시 호환, 변경 없음)·`tests/`(6개 파일, `wc -l` 재계산: conftest 39·test_cli 111·test_config 95·test_exporter 194·test_utils 265 = 704줄, 과거 로그 기재와 일치) 전량 재독 — 전체 MATCH, 신규 UNDOCUMENTED/STALE/MISSING_SOURCE 없음. `AGENTS.md`/`.cursor/rules`/`*.rule` 등 추가 규칙 문서는 레포에 존재하지 않음(`find . -maxdepth 3` 확인) — 해당 없음. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`, PUBLICATION_REVIEW.md 기재와 일치). **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분이 31일 연속, 31차(2026-08-05) 수정분이 6일째 uncommitted 유지 — 문서-코드 정합성에는 영향 없으므로 자동 커밋하지 않음(사용자 요청 시에만). |
| 2026-08-12 | Doc-Sync 35차 검증 — MISMATCH 0건. 전 회차(34차, 2026-08-11) 대비 소스 커밋·파일 변경 없음(마지막 커밋 `59206fe` 유지, `git diff --stat HEAD -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `find . -newermt "$(git log -1 --format=%cI)"` 결과 `CLAUDE.md`·`docs/GUI_SPEC.md`·`docs/GUI_TEST_CHECKLIST.md` 3개뿐 — 21차(2026-07-11)+31차(2026-08-05) 수정분이 각각 32일째/7일째 uncommitted 유지, `wc -l` 재계산: gui.py 2159·utils.py 1194·cli.py 539·config.py 294·exporter.py 767, 전 회차 기재와 일치). 독립 재검증(핵심 앵커 재대조, 34차까지 전량 재독으로 이미 확인된 항목은 대표 샘플로 축소): `self.btn_` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생, `_test_channels_parallel`(758행) 호출부 0건, `FORMAT_OPTIONS` 6종(55-62행), `CLI_FORMATS` 8종(331-335행), `validate_channel_id()`(326-346행, `isdigit()`가 길이 체크보다 우선), `safe_mode` 이중 기본값(exporter.py:41=False/gui.py:89=True), `git remote -v`(`nori00000/discord-downloader`)와 NOTICE.md/PROJECT.md/PUBLICATION_REVIEW.md 레포명 일치, `pyproject.toml`/`__init__.py` 버전 1.1.0 일치, `.github/workflows/test.yml` CI 매트릭스(ubuntu+macos py3.9-3.13+windows smoke) — 전부 MATCH. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`). `AGENTS.md`/`.cursor/rules`/`*.rule` 여전히 부재. 신규 UNDOCUMENTED/STALE/MISSING_SOURCE 없음. **메타 관찰**: 소스 커밋이 2026-07-09(`59206fe`) 이후 34일째 정지 상태이며, 22차(2026-07-14)부터 이번 35차까지 총 14회 연속 재검증에서 실질적 신규 불일치는 31차(2026-08-05) 1건뿐 — 소스가 바뀌지 않는 한 매 세션 전량 재검증의 한계효용은 사실상 0에 수렴함. 권고(강제 아님): 이후 회차는 `git diff --stat <마지막 커밋> -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example`가 비어 있으면(=소스 무변경) 전량 재대조 대신 diff 부재 확인만으로 짧게 종료하고, 소스에 실제 커밋이 발생한 회차에만 전량 재검증을 수행하는 방식으로 전환 검토. 21차(2026-07-11)·31차(2026-08-05) 수정분이 각각 32일·7일째 uncommitted 유지 — 문서-코드 정합성에는 영향 없으므로 자동 커밋하지 않음(사용자 요청 시에만). |
| 2026-08-13 | Doc-Sync 36차 검증 — MISMATCH 0건. 35차(2026-08-12) 권고대로 diff-먼저 축약 절차 적용: `git diff --stat HEAD -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `git log -1 --format="%H %cI"` = `59206fe1... 2026-07-09T15:01:39+09:00`(변동 없음), `find . -newermt "$(git log -1 --format=%cI)" -not -path "./.git/*" -not -path "./.ruff_cache/*" -not -path "./.omc/*" -type f` 결과 여전히 `CLAUDE.md`·`docs/GUI_SPEC.md`·`docs/GUI_TEST_CHECKLIST.md` 3개뿐(21차 수정분 33일째, 31차 수정분 8일째 uncommitted 유지) — 소스 무변경 확인. 핵심 앵커 스팟체크(전량 재독 대신 대표 샘플, 35차 권고 절차): `self.btn_` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생, `_test_channels_parallel`(758행) 정의만 존재·호출부 0건(dead code 잔존, 코드 수정 권고 유지·문서 수정 불필요), `FORMAT_OPTIONS`(55행)·`CLI_FORMATS`(331행) 존재 확인, `safe_mode` 이중 기본값(exporter.py:41=False/gui.py:89=True) 재확인, `__init__.py`/`pyproject.toml` 버전 `1.1.0` 일치 — 전부 MATCH. `wc -l` 재계산: gui.py 2159·utils.py 1194·cli.py 539·config.py 294·exporter.py 767, 전 회차 기재와 일치. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`, PUBLICATION_REVIEW.md 기재와 일치). `AGENTS.md`/`.cursor/rules`/`*.rule` 여전히 레포에 부재(`find` 확인) — 해당 없음. 현재 working tree의 uncommitted `docs/GUI_SPEC.md`(E9/E10 정정)·`docs/GUI_TEST_CHECKLIST.md`(시나리오 4 케이스 분리)는 21차·31차에서 이미 코드 근거로 검증된 내용 그대로이며 이번 회차에도 코드와 일치 — 추가 수정 불필요. 신규 UNDOCUMENTED/STALE/MISSING_SOURCE 없음. **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분이 33일 연속, 31차(2026-08-05) 수정분이 8일째 uncommitted 유지 — 자동 커밋하지 않음(사용자 요청 시에만). 소스 커밋 정지 35일째(마지막 커밋 `59206fe`, 2026-07-09) — 소스에 실제 변경이 발생하기 전까지는 이 축약 절차를 유지 권고. |
| 2026-08-14 | Doc-Sync 37차 검증 — MISMATCH 0건. 36차(2026-08-13) 권고대로 diff-먼저 축약 절차 적용: `git diff --stat HEAD -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `git log -1 --format="%H %cI"` = `59206fe1... 2026-07-09T15:01:39+09:00`(변동 없음), `find . -newermt "$(git log -1 --format=%cI)" -not -path "./.git/*" -not -path "./.ruff_cache/*" -not -path "./.omc/*" -type f` 결과 여전히 `CLAUDE.md`·`docs/GUI_SPEC.md`·`docs/GUI_TEST_CHECKLIST.md` 3개뿐(21차 수정분 34일째, 31차 수정분 9일째 uncommitted 유지) — 소스 무변경 확인. 핵심 앵커 스팟체크(전량 재독 대신 대표 샘플, 36차와 동일 절차): `self.btn_` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생, `_test_channels_parallel`(758행) 정의만 존재·호출부 0건(dead code 잔존, 코드 수정 권고 유지·문서 수정 불필요), `FORMAT_OPTIONS`(55행)·`CLI_FORMATS`(331행) 존재 확인, `safe_mode` 이중 기본값(exporter.py:41=False/gui.py:89=True) 재확인, `__init__.py`/`pyproject.toml` 버전 `1.1.0` 일치, `git remote -v`(`nori00000/discord-downloader`)가 NOTICE.md/PROJECT.md/PUBLICATION_REVIEW.md 레포명 표기와 일치 — 전부 MATCH. `wc -l` 재계산: gui.py 2159·utils.py 1194·cli.py 539·config.py 294·exporter.py 767, tests/ 6개 파일 704줄(conftest 39·test_cli 111·test_config 95·test_exporter 194·test_utils 265, `__init__.py` 0줄), 전 회차 기재와 일치. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`, PUBLICATION_REVIEW.md 기재와 일치). `AGENTS.md`/`.cursor/rules`/`*.rule` 여전히 레포에 부재(`find . -maxdepth 3` 확인) — 해당 없음. 현재 working tree의 uncommitted `docs/GUI_SPEC.md`(E9/E10 정정)·`docs/GUI_TEST_CHECKLIST.md`(시나리오 4 케이스 분리)는 21차·31차에서 이미 코드 근거로 검증된 내용 그대로이며 이번 회차에도 코드와 일치 — 추가 수정 불필요. 신규 UNDOCUMENTED/STALE/MISSING_SOURCE 없음. **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분이 34일 연속, 31차(2026-08-05) 수정분이 9일째 uncommitted 유지 — 자동 커밋하지 않음(사용자 요청 시에만). 소스 커밋 정지 36일째(마지막 커밋 `59206fe`, 2026-07-09) — 소스에 실제 변경이 발생하기 전까지는 이 축약 절차를 유지 권고. |
| 2026-08-18 | Doc-Sync 38차 검증 — MISMATCH 0건. 37차(2026-08-14) 이후 4일 공백, diff-먼저 축약 절차 적용: `git diff --stat HEAD -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `git log -1 --format="%H %cI %s"` = `59206fe1... 2026-07-09T15:01:39+09:00 doc-sync: 20차 검증 반영...`(변동 없음), `find . -newermt "$(git log -1 --format=%cI)" -not -path "./.git/*" -not -path "./.ruff_cache/*" -not -path "./.omc/*" -type f` 결과 여전히 `CLAUDE.md`·`docs/GUI_SPEC.md`·`docs/GUI_TEST_CHECKLIST.md` 3개뿐(21차 수정분 38일째, 31차 수정분 13일째 uncommitted 유지) — 소스 무변경 확인, `wc -l` 재계산 gui.py 2159·utils.py 1194·cli.py 539·config.py 294·exporter.py 767·tests/ 6개 파일 704줄, 전 회차 기재와 일치. 핵심 앵커 스팟체크: `self.btn_` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생, `_test_channels_parallel`(758행) 정의만 존재·호출부 0건(dead code 잔존 재확인), `FORMAT_OPTIONS`(55행)·`CLI_FORMATS`(331행) 존재, `validate_channel_id()`(326-351행, `isdigit()`가 길이 체크보다 우선 — GUI_TEST_CHECKLIST 시나리오 4 각주와 일치) 및 `_on_export_error()`(1807행, "내보내기 실패" + `f"{error}\n\n해결 방법:\n{solution}"`, GUI_SPEC E9/E10 정정과 일치) 재확인, `safe_mode` 이중 기본값(exporter.py:41=False/gui.py:89=True), `__init__.py`/`pyproject.toml` 버전 `1.1.0` 일치. 이번 회차 신규 재확인: `pyproject.toml`(`[project.optional-dependencies].dev`에 pytest/pytest-cov/ruff, `dependencies = []` 재확인)·`.github/workflows/test.yml`(`os: [ubuntu-latest, macos-latest]` × `python-version: ["3.9"..."3.13"]` 매트릭스 + 별도 `windows-smoke` 잡, README/CLAUDE.md 서술과 일치)·`PUBLICATION_REVIEW.md` 전문 재독(레포명 `nori00000/discord-downloader` `git remote -v`와 일치, `pytest not run because pytest is not installed locally` 기재가 이번 회차 `python3 -m pytest --version` 재확인 결과와 일치) — 전부 MATCH. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`). `AGENTS.md`/`.cursor/rules`/`*.rule` 여전히 레포에 부재(`find . -maxdepth 3` 확인) — 해당 없음. 현재 working tree의 uncommitted `docs/GUI_SPEC.md`(E9/E10 정정)·`docs/GUI_TEST_CHECKLIST.md`(시나리오 4 케이스 분리)는 코드와 문자 단위 일치 재확인 — 추가 수정 불필요. 신규 UNDOCUMENTED/STALE/MISSING_SOURCE 없음. **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분이 38일 연속, 31차(2026-08-05) 수정분이 13일째 uncommitted 유지 — 자동 커밋하지 않음(사용자 요청 시에만). 소스 커밋 정지 40일째(마지막 커밋 `59206fe`, 2026-07-09) — 소스에 실제 변경이 발생하기 전까지는 이 축약 절차를 유지 권고. |
| 2026-08-19 | Doc-Sync 39차 검증 — MISMATCH 0건. 38차(2026-08-18) 이후 1일 공백, diff-먼저 축약 절차 적용: `git diff --stat 59206fe -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `git log -1 --format="%H %cI %s"` = `59206fe1... 2026-07-09T15:01:39+09:00 doc-sync: 20차 검증 반영...`(변동 없음), `find . -newermt "$(git log -1 --format=%cI)" -not -path "./.git/*" -not -path "./.ruff_cache/*" -not -path "./.omc/*" -type f` 결과 여전히 `CLAUDE.md`·`docs/GUI_SPEC.md`·`docs/GUI_TEST_CHECKLIST.md` 3개뿐(21차 수정분 39일째, 31차 수정분 14일째 uncommitted 유지) — 소스 무변경 확인. `wc -l` 재계산: gui.py 2159·utils.py 1194·cli.py 539·config.py 294·exporter.py 767·tests/ 6개 파일(conftest 39·test_cli 111·test_config 95·test_exporter 194·test_utils 265, `__init__.py` 0줄) = 704줄, 전 회차 기재와 일치. 핵심 앵커 스팟체크: `self.btn_` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생, `_test_channels_parallel`(758행) 정의만 존재·호출부 0건(dead code 잔존 재확인, 코드 수정 권고 유지·문서 수정 불필요), `FORMAT_OPTIONS`(55행)·`CLI_FORMATS`(331행) 존재 확인, `validate_channel_id()`(326-351행, `isdigit()`가 길이 체크보다 우선 — GUI_TEST_CHECKLIST 시나리오 4 각주와 일치) 재확인, `safe_mode` 이중 기본값(exporter.py:41=False/gui.py:89=True) 재확인, `__init__.py`/`pyproject.toml` 버전 `1.1.0` 일치, `git remote -v`(`nori00000/discord-downloader`)가 `PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md` 레포명 표기와 일치, `PUBLICATION_REVIEW.md` 전문 재독(공개 준비 상태 `ready-for-final-visibility-approval`, `pytest not run because pytest is not installed locally` 기재 재확인) — 전부 MATCH. `.github/workflows/test.yml`(`os: [ubuntu-latest, macos-latest]` × `python-version: ["3.9".."3.13"]` 매트릭스 + 별도 `windows-smoke` 잡) 재확인. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`, PUBLICATION_REVIEW.md 기재와 일치). `AGENTS.md`/`.cursor/rules`/`*.rule` 여전히 레포에 부재(`find . -maxdepth 3` 확인) — 해당 없음. 현재 working tree의 uncommitted `docs/GUI_SPEC.md`(E9/E10 정정)·`docs/GUI_TEST_CHECKLIST.md`(시나리오 4 케이스 분리)는 코드와 문자 단위 일치 재확인 — 추가 수정 불필요. 신규 UNDOCUMENTED/STALE/MISSING_SOURCE 없음. **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분이 39일 연속, 31차(2026-08-05) 수정분이 14일째 uncommitted 유지 — 자동 커밋하지 않음(사용자 요청 시에만). 소스 커밋 정지 41일째(마지막 커밋 `59206fe`, 2026-07-09) — 소스에 실제 변경이 발생하기 전까지는 이 축약 절차를 유지 권고. |
| 2026-08-20 | Doc-Sync 40차 검증 — MISMATCH 0건. 39차(2026-08-19) 이후 1일 공백, diff-먼저 축약 절차 적용: `git diff --stat HEAD -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `git log -1 --format="%H %cI %s"` = `59206fe1... 2026-07-09T15:01:39+09:00 doc-sync: 20차 검증 반영...`(변동 없음), `find . -newermt "$(git log -1 --format=%cI)" -not -path "./.git/*" -not -path "./.ruff_cache/*" -not -path "./.omc/*" -type f` 결과 여전히 `CLAUDE.md`·`docs/GUI_SPEC.md`·`docs/GUI_TEST_CHECKLIST.md` 3개뿐(21차 수정분 40일째, 31차 수정분 15일째 uncommitted 유지) — 소스 무변경 확인. `wc -l` 재계산: gui.py 2159·utils.py 1194·cli.py 539·config.py 294·exporter.py 767·tests/ 6개 파일(conftest 39·test_cli 111·test_config 95·test_exporter 194·test_utils 265, `__init__.py` 0줄) = 704줄, 전 회차 기재와 일치. 핵심 앵커 스팟체크: `self.btn_` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생, `_test_channels_parallel`(758행) 정의만 존재·호출부 0건(dead code 잔존 재확인, 코드 수정 권고 유지·문서 수정 불필요), `FORMAT_OPTIONS`(55행)·`CLI_FORMATS`(331행) 존재 확인, `validate_channel_id()`(326-351행, `isdigit()`가 길이 체크보다 우선 — GUI_TEST_CHECKLIST 시나리오 4 각주와 일치) 재확인, `safe_mode` 이중 기본값(exporter.py:41=False/gui.py:89=True) 재확인, `__init__.py`/`pyproject.toml` 버전 `1.1.0` 일치, `git remote -v`(`nori00000/discord-downloader`)가 `PROJECT.md`/`NOTICE.md`/`PUBLICATION_REVIEW.md` 레포명 표기와 일치, `PUBLICATION_REVIEW.md` 전문 재독(공개 준비 상태 `ready-for-final-visibility-approval`, `pytest not run because pytest is not installed locally` 기재 재확인) — 전부 MATCH. `.github/workflows/test.yml`(`os: [ubuntu-latest, macos-latest]` × `python-version: ["3.9".."3.13"]` 매트릭스 + 별도 `windows-smoke` 잡) 재확인. 현재 uncommitted `docs/GUI_SPEC.md`(E9/E10 다이얼로그 서술 정정, 2026-08-05)·`docs/GUI_TEST_CHECKLIST.md`(시나리오 4 케이스 분리, 2026-07-11)를 `git diff`로 재대조 — 두 문서 모두 코드(`gui.py:_on_export_error` 1807-1817행, `utils.py:validate_channel_id` 326-351행)와 문자 단위 일치, 추가 수정 불필요. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`, PUBLICATION_REVIEW.md 기재와 일치). `AGENTS.md`/`.cursor/rules`/`*.rule` 여전히 레포에 부재(`find . -maxdepth 3` 확인) — 해당 없음. 신규 UNDOCUMENTED/STALE/MISSING_SOURCE 없음. **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분이 40일 연속, 31차(2026-08-05) 수정분이 15일째 uncommitted 유지 — 자동 커밋하지 않음(사용자 요청 시에만). 소스 커밋 정지 42일째(마지막 커밋 `59206fe`, 2026-07-09) — 소스에 실제 변경이 발생하기 전까지는 이 축약 절차를 유지 권고. |
| 2026-08-21 | Doc-Sync 41차 검증 — MISMATCH 0건. 40차(2026-08-20) 이후 1일 공백, diff-먼저 축약 절차 적용: `git diff --stat HEAD -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `git log -1 --format="%H %cI %s"` = `59206fe1... 2026-07-09T15:01:39+09:00 doc-sync: 20차 검증 반영...`(변동 없음), `find . -newermt "$(git log -1 --format=%cI)" -not -path "./.git/*" -not -path "./.ruff_cache/*" -not -path "./.omc/*" -type f` 결과 여전히 `CLAUDE.md`·`docs/GUI_SPEC.md`·`docs/GUI_TEST_CHECKLIST.md` 3개뿐(21차 수정분 41일째, 31차 수정분 16일째 uncommitted 유지) — 소스 무변경 확인. `wc -l` 재계산: gui.py 2159·utils.py 1194·cli.py 539·config.py 294·exporter.py 767·tests/ 6개 파일(conftest 39·test_cli 111·test_config 95·test_exporter 194·test_utils 265, `__init__.py` 0줄) = 704줄, 전 회차 기재와 일치. 핵심 앵커 스팟체크: `self.btn_` 9건 전부 `btn_export`(1024행)/`btn_stop`(1032행)에서만 발생, `_test_channels_parallel`(758행) 정의만 존재·호출부 0건(dead code 잔존 재확인, 코드 수정 권고 유지·문서 수정 불필요), `FORMAT_OPTIONS`(55행)·`CLI_FORMATS`(331행) 존재 확인, `validate_channel_id()`(326-351행, `isdigit()`가 길이 체크보다 우선 — GUI_TEST_CHECKLIST 시나리오 4 각주와 일치) 재확인, `safe_mode` 이중 기본값(exporter.py:41=False/gui.py:89=True) 재확인, `__init__.py`/`pyproject.toml` 버전 `1.1.0` 일치 재확인. `git diff` 재대조로 현재 uncommitted `docs/GUI_SPEC.md`(E9/E10 다이얼로그 서술 정정)·`docs/GUI_TEST_CHECKLIST.md`(시나리오 4 케이스 분리)가 코드(`gui.py:_on_export_error` 1807-1817행, `utils.py:validate_channel_id` 326-351행)와 문자 단위 일치함을 재확인 — 추가 수정 불필요. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`, PUBLICATION_REVIEW.md 기재와 일치). `AGENTS.md`/`.cursor/rules`/`*.rule` 여전히 레포에 부재(`find . -maxdepth 3` 확인) — 해당 없음. 신규 UNDOCUMENTED/STALE/MISSING_SOURCE 없음. **관찰(문서 정합성 외 사안, 수정 불필요)**: 21차(2026-07-11) 수정분이 41일 연속, 31차(2026-08-05) 수정분이 16일째 uncommitted 유지 — 자동 커밋하지 않음(사용자 요청 시에만). 소스 커밋 정지 43일째(마지막 커밋 `59206fe`, 2026-07-09) — 소스에 실제 변경이 발생하기 전까지는 이 축약 절차를 유지 권고. |
| 2026-08-25 | Doc-Sync 42차 검증 — MISMATCH 0건. 41차(2026-08-21) 이후 4일 공백, diff-먼저 축약 절차 적용: `git diff --stat HEAD -- discord_exporter/ tests/ pyproject.toml .github/ setup.py .env.example` 결과 0건, `git log -3 --format="%H %cI %s"` 최상단 여전히 `59206fe1... 2026-07-09T15:01:39+09:00 doc-sync: 20차 검증 반영...`(변동 없음), `find . -newermt "$(git log -1 --format=%cI)" -not -path "./.git/*" -not -path "./.ruff_cache/*" -not -path "./.omc/*" -type f` 결과 여전히 `CLAUDE.md`·`docs/GUI_SPEC.md`·`docs/GUI_TEST_CHECKLIST.md` 3개뿐(21차 수정분 45일째, 31차 수정분 20일째 uncommitted 유지, 소스 커밋 정지 47일째) — 소스 무변경 확인. `wc -l` 재계산: gui.py 2159·utils.py 1194·cli.py 539·config.py 294·exporter.py 767·`__init__.py` 23·tests/ 6개 파일(conftest 39·test_cli 111·test_config 95·test_exporter 194·test_utils 265·`__init__.py` 0줄) = 704줄, 전 회차 기재와 일치. 핵심 앵커 재스팟체크: `validate_channel_id()`(utils.py:326-351행, `isdigit()`가 길이 체크보다 우선 — GUI_TEST_CHECKLIST 시나리오 4 각주와 문자 단위 일치) 재확인, `_on_export_error()`(gui.py:1807-1817행) `messagebox.showerror("내보내기 실패", f"{error}\n\n해결 방법:\n{solution}")` 재확인 — GUI_SPEC E9/E10 서술과 일치, `_test_channels_parallel`(gui.py:758행) 정의만 존재·호출부 0건(dead code 잔존 재확인, 코드 수정 권고 유지·문서 수정 불필요), `filter_accessible` 관련 `ThreadPoolExecutor(max_workers=10)`(gui.py:661행) 재확인 — GUI_SPEC §2 표 기재와 일치, `__version__ = "1.1.0"`(`__init__.py`:20행)·`pyproject.toml` `version = "1.1.0"` 일치 재확인. `git diff` 재대조로 현재 uncommitted `docs/GUI_SPEC.md`(E9/E10 다이얼로그 서술 정정)·`docs/GUI_TEST_CHECKLIST.md`(시나리오 4 케이스 분리)가 코드와 문자 단위 일치함을 재확인 — 추가 수정 불필요. `ruff check discord_exporter/ tests/` 신규 실행 clean(All checks passed!, exit 0), `python3 -m pytest --version` 로컬 미설치 재확인(`No module named pytest`, PUBLICATION_REVIEW.md 기재와 일치). `AGENTS.md`/`.cursor/rules`/`*.rule` 여전히 레포에 부재(`find . -maxdepth 3` 확인) — 해당 없음. 신규 UNDOCUMENTED/STALE/MISSING_SOURCE 없음. |
